# Nunch Skills 문서 사이트

Starlight 기반의 독립 문서 패키지입니다. 원본 프로젝트 문서는 저장소의 `docs/`에 유지하고, 사이트용 콘텐츠는 `src/content/docs/`에서 관리합니다.

## 실행

```bash
pnpm dev
pnpm build
pnpm preview
```

`pnpm build`는 정적 사이트 생성과 내부 링크 검증을 함께 실행합니다.
